import { default as create } from './create';
import { default as getImages } from './getImages';
import { default as getImage, getImageMiddleware } from './getImage';
import { default as remove } from './remove';

export default {
  create,
  getImages,
  getImageMiddleware,
  getImage,
  remove,
};
