import { default as create } from './create';
import { default as getComponent, getComponentMiddleware } from './getComponent';

export default {
  create,
  getComponent,
  getComponentMiddleware,
};
